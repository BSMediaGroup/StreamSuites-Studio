# Bump Notes

## CURRENT VER= 0.5.0-alpha / PENDING VER= 0.5.1-alpha

- Stage capacity correction: Studio now displays nine total Stage slots, one reserved director slot, and eight additional guest/cohost slots. Admission controls disable before a ninth additional optimistic move, recover as soon as someone moves Backstage, and render the safe Runtime `stage_capacity_reached` response without exposing backend details.
- Layout alignment: Grid, Interview, Spotlight, Presentation, fullscreen, and cinematic rendering use the same nine-total/eight-additional ceiling, keep Stage order and Backstage/participant menus intact, and never render a tenth Stage tile. RealtimeKit Grid Size remains `9` and no preset or environment value changed.
- Stage-capacity validation: `npm run check`, focused/full Vitest coverage, lint, build, and `git diff --check` were run for this correction; no live Runtime/Auth process or Cloudflare API was used.

- RealtimeKit Core milestone: installed the official Cloudflare Core/React SDK 2.0.0 packages, added a room-scoped in-memory lifecycle, explicit participant-token provisioning, connect/leave cleanup, truthful media status/error states, and functional local microphone, camera, and screen-share controls while preserving the custom Stage/Backstage workspace and SSE connection.
- Security and scope: Runtime/Auth remains canonical; no Cloudflare API token or configuration enters Studio, participant tokens are never persisted, hosts cannot force-enable another participant's hardware, and OFF AIR remains unchanged. Remote tile registration, full device preflight, provider Stage/host synchronization, and signed webhooks remain follow-up work; recording/broadcast/LiveKit/Egress were not added.

- Invite-page Auth: replaced the full-page sign-in departure with a focus-trapped responsive sheet that reuses the existing Turnstile, development bypass, password, OAuth, error, and loading implementation. Password auth refreshes in place; OAuth stores only display name, subtitle, supported initials color, and a 15-minute timestamp before returning to the exact invite route. Closing the sheet preserves the form and anonymous join remains available.
- Guest identity: replaced the initials-color select with keyboard-accessible visual swatches. Guest avatar previews remain temporary object URLs, while canonical responses accept only versioned `https://cdn.streamsuites.app/...` URLs and never render data/local paths.
- Guest room experience: successful join now navigates to the canonical short-code room workspace. Backstage guests can see the safe Stage plus their horizontal Backstage tile; ordinary guests cannot self-admit but can move themselves Backstage, and authorized cohosts can move themselves both ways.
- Participant controls: added a horizontal Backstage tray, permission-aware Stage menus, distinct move-Backstage and confirmed remove-from-room actions, Runtime-backed intended microphone/camera state, director/cohost Stage/Backstage moves, optimistic drag ordering with rollback, and keyboard earlier/later ordering.
- Cohost and presentation management: expanded session/permanent controls into an explicit CO-HOSTS section with invitation status, scope summaries/changes, session revoke, and permanent revoke. Added Runtime-synchronized Presentation layout with the truthful `Presentation source not connected` placeholder.
- Shell/UI corrections: added canonical Room ID chips, avatar/name/role/admin/tier account-chip assets with no arrow, unique committed SVG sidebar icons, a slim Runtime version/health footer, a full-height Rooms/exit button with committed icon, and exact ROOM DETAILS, STAGE OUTPUT, WAITING BACKSTAGE, ON STAGE, and CO-HOSTS label cleanup. Slim header keeps the StreamSuites wordmark and hides only the Studio subtitle.
- Explicit no-media boundary: intended microphone/camera state does not request device permission or operate hardware/tracks. No screen capture, WebRTC, Cloudflare Realtime, recording, broadcast, LiveKit, or Egress behavior was added; Stage remains OFF AIR.
- Validation: `npm run check`, `npm run lint`, the full Vitest suite, `npm run build`, and `git diff --check` passed for the completed milestone.
- Human note: Studio now behaves like a coherent pre-media control room while Runtime/Auth remains the sole participant, location, order, presentation, cohost, invite, and control authority.

- Configurable Studio chrome: enhanced the fixed expanded sidebar into expanded, compact icons-only, and hidden desktop modes, and the fixed-height topbar into standard, materially slimmer, and delayed auto-hide modes. A keyboard-accessible View menu exposes the selected values; compact items retain names, titles, active state, focus treatment, and unavailable Later semantics, while mobile continues to use the existing closable drawer and auto-hide safely falls back to slim.
- Cinematic room workspace: added a route-scoped stage-first mode that retracts nonessential shell framing without remounting the room, preserves `OFF AIR`, room/lifecycle identity, Stage layout/labels, Runtime/SSE connection truth, arrival counts, essential production controls, and obvious `F`/button exits. The refined centered dock retains truthful disabled microphone, camera, screen-share, and Go live behavior and adds Backstage access with waiting/on-stage counts.
- Cinematic Backstage and room tools: the existing Runtime-fed Backstage/invite/settings panel and mutation handlers now become a focus-managed overlay/sheet in cinematic mode. Immediate SSE-refetched arrivals, admit/deny/remove/cohost actions, waiting attention, Escape close, outside/scrim close, and focus return use the same component state and permission contract; no parallel lobby or authority store was added.
- Fullscreen and local preferences: added explicit standard Fullscreen API handling against the room workspace, actual `fullscreenchange` reflection, rejection-safe messaging, on-screen exit, and listener cleanup. Only validated sidebar/header/cinematic presentation fields persist under `streamsuites_studio_presentation`; fullscreen, accounts, rooms, invites, guests, permissions, SSE, and media state never persist there.
- Accessibility/mobile: added reduced-motion-safe chrome transitions, a keyboard-reachable auto-hide activation strip, menu/drawer Escape and focus recovery, cinematic focus trapping, accessible compact navigation labels, persistent hidden-sidebar restoration, touch-safe mobile fallbacks, and narrow-screen cinematic sheets/docks.
- Authority/media preservation: Runtime/Auth remains canonical and the existing room component and SSE connection are not recreated solely by chrome changes. Cinematic mode remains pre-media and never claims output is live; no camera, microphone, WebRTC, Cloudflare Realtime, recording, broadcasting, LiveKit, Egress, or destination implementation was added.
- Validation: `npm run check`, `npm run lint`, all 38 Vitest tests, `npm run build`, `git diff --check`, production-preview direct SPA requests, and a controlled Playwright production-browser proof passed. Desktop proof measured expanded/compact/hidden sidebar widths at 245/72/0px, standard/slim headers at 72/54px, delayed auto-hide plus pointer reveal, cinematic Stage/`OFF AIR`/SSE/waiting-count continuity, Backstage drawer focus return, fullscreen control presence, and zero console errors/warnings. The 390px dark/light mobile proof retained explicit navigation, cinematic exit/Backstage/dock access, and 390/390 document width with no horizontal overflow. Native browser fullscreen was policy-limited in CLI; success/change/rejection/cleanup paths passed controlled Fullscreen API tests instead. Browser room/account responses were controlled route stubs, so deployed Runtime/media integration is not claimed.
- Human note: Studio can now recede from a full management shell into a deliberate cinematic control room while keeping every visible operational fact grounded in Runtime/Auth and every media control honestly unavailable.

- Real-time Studio state: the existing typed adapter now opens credentialed room/guest SSE, exposes live/reconnecting/fallback-polling/unavailable state, coalesces event bursts into authoritative refetches, polls only while disconnected, and preserves manual refresh. Backstage arrivals and guest profile/admit/deny/remove/leave/expiry changes no longer require a page refresh.
- Canonical links and invite policy UI: room routes replace old UUID URLs with Runtime-returned short codes; invite creation now supports single-use, capped, or open entry, 24-hour default expiry or no-expiry, use/exhaustion status, and copyable existing canonical links without browser persistence.
- Polished Backstage: strengthened waiting/on-stage/cohost hierarchy, connection and capacity indicators, room-scoped avatars/initials/subtitles, signed-in/guest identity, arrival animation with reduced-motion parity, accessible live announcements, focused mutation recovery, mobile-safe actions, and no fake device/camera/microphone state.
- Guest entry: `/join/:inviteCode` now shows safe director/policy/expiry context, keeps `Join as guest` primary, provides a same-invite StreamSuites sign-in path, accepts display name/subtitle/initials color/PNG-JPEG-WebP fallback avatar, and supports live guest-state plus editable room identity without persisting invite codes, credentials, avatar bytes, or authority.
- Cohost UI/contracts: added typed session cohost grant/revoke, authenticated permanent invitation accept/decline, all-room/selected-room scope update/revoke, permission summaries, and director/session/permanent Backstage presentation. Cohost controls never imply ownership, account role, tier, billing, or ALPHA grant changes.
- Presentation setting: authorized directors/cohosts can toggle participant subtitle visibility; the Stage and Backstage render room-scoped subtitles only when Runtime enables them.
- Explicit no-media boundary: no camera, microphone, screen share, WebRTC, Cloudflare Realtime, recording, RTMP, SRT, HLS, LiveKit, Egress, or broadcasting implementation was added. `OFF AIR` and disabled media controls remain truthful.
- Human note: the pre-media Studio workspace now feels live and collaborative because Runtime/Auth events drive immediate refetches, while all canonical room, invite, guest, cohost, presentation, and event state remains server-owned.

- OAuth icon production repair: replaced root-relative `/assets/icons/*.svg` references that Vite did not emit with imported URLs for the existing committed `google.svg`, `github.svg`, `discord.svg`, `x.svg`, and `twitch.svg` files. These files are byte-identical to Public's working provider assets; Vite now emits even small imported assets instead of inlining them so all five compile to independently requestable fingerprinted SVG files with consistent visible icon treatment in dark and light themes.
- Enter room flow: replaced the dominant Manage-only room rows with polished room cards showing confirmed description, lifecycle, update time, Backstage count, and on-stage count against Runtime's eight additional slots / nine total including the director. Active/draft/closed rooms have a primary `Enter room` route action; ended rooms remain visible without an active entry action.
- Pre-media room workspace: expanded the existing protected room route into a responsive production-oriented workspace with a room status strip, Runtime authority indicator, lifecycle actions, central Stage/Program canvas, safe-area guides, confirmed host/director and admitted-guest identity placeholders, meaningful open positions, and explicit awaiting-media treatment.
- Backstage and authority actions: replaced the text-heavy waiting/admitted summaries with distinct Waiting backstage and On stage guest cards, initials, confirmed timestamps, per-guest pending states, focus recovery, stage-full messaging, and authoritative room/lobby refetches after admit, deny, and remove mutations.
- Production orientation: added a dock for Microphone, Camera, Screen share, Layout, Invite, Settings, and Go live. Device controls are disabled with accessible unavailable explanations; `OFF AIR` and inactive `00:00:00` never become live, and Go live opens a non-destructive explanation that output integration is not connected.
- Local layouts and secure room tools: Grid, Interview, and Spotlight remain component-only presentation state. The earlier one-time invite warning is superseded by the canonical copyable invite-policy UI above; active/revoked/exhausted lists, revoke confirmation, room details, open/close/end, refresh/retry, and final-ended behavior remain integrated without a parallel store or route.
- Authority and media boundary: Runtime/Auth remains the sole room, lifecycle, ownership, invite, guest, lobby, admission, and capacity authority. Studio persists no room, guest, invite, stage, or raw-code truth. No camera, microphone, screen share, WebRTC, Cloudflare Realtime, recording, RTMP, SRT, HLS, LiveKit, Egress, or broadcast implementation was added; Cloudflare Realtime remains the next media milestone.
- Validation: `npm run check`, `npm run lint`, all 32 Vitest tests, `npm run build`, and `git diff --check` passed. Production preview returned 200 for `/`, `/login`, `/studio`, `/studio/rooms/:roomId`, `/join/:inviteCode`, and all five fingerprinted SVG assets with `image/svg+xml`. Two controlled Playwright browser proofs passed across dark/light desktop and 390px mobile: all provider images returned 200 with nonzero natural/rendered dimensions, Enter room was visible, Stage and Backstage rendered together on desktop, mobile had no horizontal overflow, disabled media controls made zero permission requests, and no Studio console warning/error remained.
- Human note: entering a room now feels like entering StreamSuites Studio while every visible participant, lifecycle, and lobby decision remains grounded in Runtime/Auth and the production surface stays honestly off air.

- Turnstile lifecycle repair: replaced the unguarded render completion sequence with a single-generation lifecycle. The widget now publishes challenge-required before provider render, cannot overwrite a synchronous completion callback afterward, ignores stale callbacks, shares successful config/script loads, removes exactly one owned widget on theme replacement/unmount, and keeps completed tokens through unrelated rerenders.
- Runtime development-access parity: Studio now consumes the existing credentialed `GET /auth/access-state` and `POST /auth/debug/unlock` `{ code }` contract, preserves Runtime's signed short-lived HttpOnly bypass cookie, refreshes the public-safe gate state after unlock, expires its in-memory UI reflection, and keeps Turnstile as a separate required control. The bypass code and Turnstile token are never logged or written to browser storage.
- Login and shell parity: OAuth buttons now use the committed Google, GitHub, Discord, X, and Twitch SVG icons with labels; the favicon reference now uses `/assets/icons/studiofavicon.ico`; a reduced-motion-aware, reference-counted gradient bar sits directly below public and Studio headers; and the signed-in topbar now uses Runtime avatar/display/account metadata with initial fallback, click-outside/Escape closing, focus restoration, and authoritative logout.
- Access messaging: Runtime-provided maintenance/development copy and `show_lockout_banner` now render without turning Studio into an access authority. Unavailable state, invalid/rate-limited bypass, bypass accepted/expired, Turnstile phases, credential errors, OAuth start, and closed-ALPHA denial remain distinct.
- Validation: `npm run check`, `npm run lint`, 28 full-suite tests plus 2 focused login-parity tests, and `npm run build` passed. Direct preview requests returned 200 for `/`, `/login`, `/studio`, `/studio/rooms/:roomId`, `/join/:inviteCode`, and the built `image/x-icon` favicon; `git diff --check` passed. The available browser CLI stalled and was terminated, so no interactive desktop/mobile or deployed Turnstile/OAuth success is claimed.
- Human note: Studio login now follows the same Runtime-owned access controls and recognizable auth/header treatment as the established StreamSuites surfaces without changing rooms, invites, lobby authority, or adding media.

- Studio room dashboard: replaced the empty authorized `/studio` scaffold with Runtime/Auth-backed room loading, truthful loading/empty/error states, room creation, safe lifecycle/count summaries, open actions, and management links. Public accounts keep successful Studio access but see invite-participation guidance instead of owner controls.
- Room management route: added protected `/studio/rooms/:roomId` with editable title/description, open/close/final-end controls, nine-total/eight-additional Stage capacity truth, Backstage/on-stage counts, refresh/retry behavior, accessible live status messages, and explicit no-media copy.
- Invite lifecycle foundation (superseded in this bucket): the earlier UI received a raw guest URL once. The current UI instead receives authorized Runtime-regenerated short canonical codes in safe invite lists, supports repeated copy plus policy/use/expiry state, and still never persists a code in browser storage.
- Guest join flow: replaced the `/join/:inviteCode` placeholder with POST-body validation, normalized display-name entry, temporary guest join, waiting/admitted/denied/removed/left/expired states, self-state refresh, and leave behavior. The shared account session remains intact and no signup is required.
- Host lobby controls: waiting guests can be admitted or denied; admitted guests can be removed; duplicate submissions are disabled, stage-full conflicts are surfaced from Runtime/Auth, and admission language never claims that camera/microphone/media is connected.
- Typed Studio API adapter: extended the existing credentialed/cancellable Runtime/Auth adapter with normalized room, lifecycle, invite, guest-session, lobby, and machine-readable error contracts. No canonical room/invite/lobby state, raw invite code, or guest credential is stored in browser storage or logged.
- Responsive/accessibility polish: added desktop/mobile room, invite, and lobby layouts; preserved dark/light themes; retained focus-safe native confirmations and selectable copy fallback; added live-region status messages; and requested no media permissions.
- Explicit no-media boundary: no camera, microphone, screen share, audio/video, Cloudflare Realtime, LiveKit, Egress, recording, RTMP, SRT, HLS, or broadcast delivery was implemented or tested. Cloudflare Realtime remains the next media layer.
- Validation: TypeScript check, ESLint, all 21 Vitest tests, production build, both repository diff checks, bundle secret-field scan, and controlled desktop/390px browser route smoke pass. Direct room/join routes, dark/light mode, mobile navigation, zero horizontal overflow, and zero controlled-run console warnings/errors were verified without claiming deployed Runtime/Auth or media proof.
- Human note: Studio now coordinates real Runtime/Auth-owned rooms and guest admission while remaining honest that admission is authority only, not a live call or broadcast.

- Turnstile login parity: `/login` now uses the established explicit-render Runtime/Auth configuration contract for password login and the Google, GitHub, Discord, X, and Twitch OAuth starts. Studio sends only the confirmed `turnstile_token`; backend endpoint action labels remain `login_password`, `login_google`, `login_github`, `login_discord`, `login_x`, and `login_twitch`, with no invented widget action value.
- Ephemeral challenge lifecycle: login actions remain locked until required fields and a current token are ready. Tokens stay in component memory and are cleared on expiry, widget failure, theme re-render, failed password authentication, and backend Turnstile rejection; Strict Mode, route unmount/remount, retry, and single-script cleanup paths have focused coverage.
- Theme and layout parity: the widget renders with explicit dark/light appearance from the existing Studio theme, safely replaces its instance on theme changes without clearing form input, and uses a bounded accessible container for mobile width and reduced-motion-compatible layout.
- Truthful auth errors: loading, challenge-required, ready, expired, widget-failed, provider-unavailable, backend-verification-rejected, invalid-credentials, Runtime/Auth-unavailable, and existing post-auth closed-ALPHA denial states remain distinct. Provider failures expose a keyboard-accessible retry without being labeled as credential failure.
- Environment contract: Studio reads the public site key from Runtime/Auth `GET /auth/turnstile/config`; there is no invented `VITE_*` site-key variable. Cloudflare Pages configures the existing public `VITE_RUNTIME_API_BASE_URL`, while Runtime/Auth retains `CLOUDFLARE_TURNSTILE_SITEKEY`, `CLOUDFLARE_TURNSTILE_SECRET`, and `CLOUDFLARE_TURNSTILE_ENABLED`. No real keys or secrets were added.
- Validation completed: `npm run check`, `npm run lint`, focused Vitest coverage, the full Studio test suite, production build, controlled `/login` browser checks with mocked Runtime/Auth public config/widget responses in dark/light desktop/mobile layouts, and `git diff --check`. No deployed Cloudflare challenge was claimed.
- Human note: Studio now presents the same runtime-authorized security checkpoint as the existing StreamSuites auth surfaces without creating a new login authority, persisting challenge tokens, or weakening the closed-ALPHA access decision.

- Real Runtime/Auth bridge: replaced the placeholder-only current-session seam with typed, credentialed `GET /auth/session` and `GET /api/studio/access` requests against the established public API origin, including cancellation and safe error normalization without persisting account or grant truth in browser storage.
- Protected Studio route: `/studio` now shows a neutral loading state, redirects unauthenticated visitors to `/login` with a safe local return path, renders the existing shell only after access is allowed, and distinguishes denied, account-restricted, and retryable Runtime/Auth-unavailable states without an authorized-shell flash.
- Existing login/logout integration: `/login` now uses the implemented Runtime/Auth Google, GitHub, Discord, X, Twitch, and email/password entry paths, explains that existing StreamSuites accounts are reused, checks Studio access after authentication, and uses the existing `POST /auth/logout` authority.
- Closed-ALPHA policy UI: admins and granted creator/developer/public accounts consume the runtime response; non-granted accounts receive a clear admin-managed ALPHA message and outages are never presented as denial. No self-service ALPHA application was added.
- Theme milestone: expanded the design-token architecture into accessible dark and light modes across public, login, denial/error, and Studio shells. Dark is the first-visit default; only `streamsuites_studio_theme` is stored locally, the toggle exposes switch semantics, and the early head script prevents an obvious wrong-theme flash.
- Correct Studio branding: replaced the previous `assets/logos/logoshield.svg` header import with the verified existing `assets/logos/sscmattesilver.webp` asset, preserving its square aspect ratio on desktop and mobile in both themes.
- Explicit non-capability note: rooms, guest-invite validation, Cloudflare Realtime, LiveKit, camera/microphone/screen share, broadcasting, recording, and OBS output remain unimplemented.
- Human note: Studio now truthfully opens only when the existing StreamSuites account and runtime-owned ALPHA policy agree, while the media workspace remains an honest empty foundation.

- Studio version authority: StreamSuites Runtime remains the sole product version source. This private package uses `0.0.0` only as non-authoritative npm metadata; the UI shows the ALPHA stage while numeric hydration waits for a confirmed Studio-safe publication path for the runtime `version.json` export.
- Initial frontend scaffold: added a React + TypeScript + Vite foundation with application bootstrap, clean browser routing, reusable public and Studio shells, focused UI primitives, provisional Runtime/Auth adapters and domain types, tokenized responsive dark styling, and focused tests.
- Initial routes: added `/`, `/login`, `/studio`, `/join/:inviteCode`, and a not-found route. Each surface is explicit about closed access and unavailable backend/media behavior; no fake authenticated state, active rooms, participants, analytics, broadcasts, or invite validation is shown.
- Cloudflare Pages readiness: added the `dist` production build target, `public/_redirects` SPA fallback, and a public-safe `.env.example` without secrets, account IDs, credentials, or tokens. No Pages project, DNS, deployment, or account-specific configuration was created.
- Documentation milestone: replaced the placeholder README with current capability, non-capability, setup, authority, media-direction, OBS-output, roadmap, and complete scaffold-tree documentation; added focused system-architecture and ALPHA-roadmap documents.
- Explicit non-capability note: authentication, room APIs, invite validation, WebRTC, Cloudflare Realtime integration, LiveKit, Egress, broadcasting, provider destinations, recording, and OBS program output are not implemented in this milestone.
- Human note: Studio now has an honest, testable flagship-product shell ready for the next existing-Auth/session bridge task, while Runtime/Auth keeps all canonical authority and media remains outside the Python runtime.
